import Home from "@/src/screens/Home";

// The public music-site home page. Rendered inside app/(site)/layout.tsx,
// which provides AppProvider + the Sidebar/Header/PlayerBar chrome.
export default function HomePage() {
  return <Home />;
}
